***REMOVED*** AGENTS.md —— 跨平台薄入口（{{platform}}）

> 由 harness-kit v{{kitVersion}} 生成。本文件是 AGENTS 类平台（codex/opencode/pi 等）的通用入口，只做路由，不复制正文。

- 完整行为锚点：`.claude/CLAUDE.md`（Claude）—— 角色 / 红线 / 术语 / C13 方法论
- 规则：`.claude/rules/`
- 模板元层：`.claude/templates/`（`理念-*.md` / `方法-*.md` / `工程实践-*.md`）
- 受管与漂移：`.harness-kit/lock`（kit 管理，别手改）

规则只写纯 Markdown，保跨平台可读（C11）。
